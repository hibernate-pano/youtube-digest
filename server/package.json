{
  "name": "youtube-digest-server",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backend: GitHub OAuth, per-user notes/vocabulary/review sync for YouTube Digest.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bash scripts/deploy.sh",
    "test": "node --test tests/*.test.js",
    "db:migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
